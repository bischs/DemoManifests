apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
spec:
  rules:
  - host: localhost
    http:
      paths:
      - path: /demo-app
        pathType: ImplementationSpecific
        backend:
          service:
            name: demo-service
            port:
              number: 80
